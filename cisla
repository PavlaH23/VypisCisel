x = int(input("Zadej libovolné číslo: "))
zacatek = x * (-1)
konec = x

for i in range(zacatek, konec + 1):
    if i < 0:
        print(f"{i:0.2f}")
    else:
        print(f"{i:>010}")

"""
jak osetrit, aby mohl uzivatel zadavat i zaporna cisla?

if x < 0:
    print(f"{i:10.2f}")
else:
    print(f"{i:>010}")
    """
